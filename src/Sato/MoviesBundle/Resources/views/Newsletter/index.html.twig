{% extends '::base_admin.html.twig' %}

{% block body -%}
	<h1>Newsletters</h1>

	<table class="records_list table table-striped table-condensed">
		<thead>
			<tr>
				<th>Id</th>
				<th>Email</th>
				<th>Createdat</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
		{% for entity in entities %}
			<tr>
				<td><a href="{{ path('admin_newsletter_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
				<td>{{ entity.email }}</td>
				<td>{% if entity.createdAt %}{{ entity.createdAt|date('d/m/Y H:i:s') }}{% endif %}</td>
				<td>
				</td>
				<td>
					<a class="btn btn-default btn-xs btn-success" href="{{ path('admin_newsletter_show', { 'id': entity.id }) }}">show</a>
				</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>

	{% endblock %}

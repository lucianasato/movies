{% extends '::base_admin.html.twig' %}

{% block body -%}
    <h1>Actor edit</h1>

    <form action="{{ path('admin_actor_update' , { 'id' : entity.id }) }}" method="POST" {{ form_enctype(edit_form) }}>
        {{ form(edit_form) }}
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_actor') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
